# 项目:开发环境管理器(暂定名)

一个 Windows 工具:从国内镜像(华为云、清华 TUNA 等)高速下载开发工具(JDK/Node/Python/Maven/数据库),可携化安装、多版本切换,并以"单入口 PATH + Junction + 历史回滚"的方式保持环境变量干净。

- 需求分析:`需求分析.md`(v0.1 讨论稿,含同类方案对比、技术候选、MVP 建议、可行性调研)
- **产品文档:`产品文档.md`(v1.1,MVP 功能规格/环境变量策略/里程碑 —— 产品层以此为指导)**
- **技术手册:`技术手册.md`(v1.0,最终技术栈 Electron+Vue+全栈TS、工程结构、系统层细则、测试门禁 —— 编码以此为准;技术冲突处以技术手册覆盖产品文档)**
- 当前阶段:M1 已完成(2026-09-07,core 九模块 + 65 测试全绿 + env 真机自检过,tag `v0.2-m1`);下一任务 M2(electron-vite 脚手架 + preload 契约 + 首跑向导/商店/已安装三页)
- M2 注意:本机 GitHub 不可直连(M0 实测),装 Electron 二进制须走镜像:`export ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`
- 语言:交流与文档均用中文
- 目标平台:Windows 11 x64 优先

## 新会话开工指引

1. 阅读顺序:本文件 → `技术手册.md`(编码为准)→ `产品文档.md`(功能为准);需要背景/理由再翻 `需求分析.md`;
2. 第一个任务:**M2 脚手架与界面骨架**(技术手册 §13);M0 探测(`catalog/`、`m0-results.json`)与 M1 core(`src/main/core/`,§4 铁律有 arch.test 把关)均已定稿,直接复用勿重写;
3. 仓库事实:git 身份为仓库内占位符 `wljs@local`(推送到远端前需改为真实邮箱);尚无远端;工程根=仓库根(§2),M1 已建 package.json/vitest/tsconfig——M2 用 electron-vite 模板**合并进根目录**(扩展 package.json 的 main/scripts/依赖,不要新起工程);
4. 遵守下方强制执行规范。


下面这段文字不许更改：
强制执行规范：
阶段性执行：每次仅执行当前指定的里程碑（如 M0），所有验收标准达成后必须立即停止，输出阶段性总结并等待用户确认，严禁自动跨入下一个里程碑。
改前必存：修改代码前必须先 Git commit 存档，确保可回退。
交前必测：交付前必须自写程序测试，通过后方可提交验收。
