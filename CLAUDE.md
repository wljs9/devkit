# 项目:开发环境管理器(暂定名)

一个 Windows 工具:从国内镜像(华为云、清华 TUNA 等)高速下载开发工具(JDK/Node/Python/Maven/数据库),可携化安装、多版本切换,并以"单入口 PATH + Junction + 历史回滚"的方式保持环境变量干净。

- 需求分析:`需求分析.md`(v0.1 讨论稿,含同类方案对比、技术候选、MVP 建议、可行性调研)
- **产品文档:`产品文档.md`(v1.0,MVP 功能规格/环境变量策略/技术选型/里程碑 —— 开发以此为指导,与需求分析冲突处以产品文档为准)**
- 当前阶段:MVP 方案已定,准备进入 M0(镜像可行性探测)
- 语言:交流与文档均用中文
- 目标平台:Windows 11 x64 优先

为确保代码库的稳定性与交付质量，在执行任何开发任务时，必须严格遵守以下前置与后置检查流程：
变更前的版本控制（Pre-change Version Control）
在实施任何代码修改、重构或配置调整之前，必须先确认当前工作区状态，并执行 Git 提交（Commit）或创建分支（Branch）以保存当前快照。严禁在未建立回退点（Fallback Point）的情况下直接修改核心逻辑，以确保在出现异常时能够无损回滚至安全状态。
交付前的自动化验证（Pre-delivery Verification）
在向用户提交最终成果前，必须编写并执行相应的测试程序（单元测试、集成测试或验证脚本）。只有在所有测试用例通过、且确认无回归错误（Regression）后，方可将结果交付给用户验收。禁止在未经验证的情况下直接交付“未经测试”的代码或方案。
