# DevKit — 产品文档

> 版本:v1.2(开发指导文档) 日期:2026-09-07
> 产品名 DevKit 为暂定名,可随时改;本文件是 MVP 开发的唯一指导文档,与《需求分析.md》冲突处以本文件为准。
> **v1.1 修订**:技术选型改为 Electron + 全栈 TypeScript,细则见《技术手册.md》;本文 §7/§8 保留原评估作历史备查,以技术手册为准。
> **v1.2 修订**:§5 按 M0 实测重写——清华 adoptium 路径不存在(改 USTC)、GitHub 直连不可达(ghfast.top 代理实测可用)、Maven 校验和实为官方源 `.sha512`(镜像目录不带校验文件)、Node 校验文件名笔误更正。证据:`m0-results.json`,定稿值:`catalog/*.json`。

## 1. 产品定位

**一句话**:Windows 桌面软件 —— 从国内镜像高速下载开发工具,可携化安装、秒级切换版本,并让环境变量永远干净、可回滚。

**三个支柱**(所有功能决策的依据):
1. **镜像优先**:华为云/清华等国内源是默认下载渠道;
2. **可携化**:不跑安装向导、不写注册表(环境变量除外),一切皆"解压到统一目录 + Junction 指向";
3. **环境变量卫生学**:PATH 终身只被本工具添加固定的 3~4 条,切换版本不动 PATH;任何写入前备份、可一键回滚。

## 2. 目标用户与核心场景

主用户:Windows 11 上频繁搭建/调整开发环境的开发者。

| 场景 | MVP 是否覆盖 |
|------|:---:|
| 从目录页一键装某工具的指定版本(镜像下载→校验→解压→可执行) | ✅ |
| 多版本并存、秒级切换当前版本 | ✅ |
| 卸载无残留 | ✅ |
| PATH 审计(失效项标红)+ 清理 + 变更回滚 | ✅ |
| 新机批量装机(manifest) | ❌ 后续 |
| 项目级(按目录)自动切换 | ❌ 后续 |
| 数据库 / Python 等安装器型软件 | ❌ 后续 |

## 3. 产品原则(红线)

1. **默认只碰用户级环境变量(HKCU)**,动系统级必须显式二次确认;
2. **任何环境变量写入前自动备份**,写入失败自动回滚;
3. **绝不自动删除非本工具创建的 PATH 条目**,体检只标出问题、清理需用户勾选确认;
4. 卸载/删除文件前预览影响面并要求确认;
5. 校验和不匹配 → 默认拒绝安装(可高级选项强制跳过,需警告);
6. 纯本地软件:不收集、不上传任何用户环境信息,无遥测。

## 4. MVP 功能规格(页面级)

应用框架:**侧边栏 4 页 + 顶栏下载指示**。

### 4.0 首次运行向导
- 步骤 1:选择安装根目录 `DevRoot`(默认探测:若存在 D 盘则 `D:\dev`,否则 `%USERPROFILE%\dev`);展示将创建的目录结构;
- 步骤 2:将 `<DevRoot>\bin\...`(见 §6 环境变量策略)写入用户 PATH —— 展示 diff(改前/改后)再确认;
- 向导全程可在设置中重跑;若用户拒绝写 PATH,软件仍可安装/切换,但顶栏常驻黄色提示"环境未接入"。

### 4.1 页面「软件商店」(首页)
- 工具卡片列表(MVP 内置:**Node.js、JDK(Temurin)、Maven**),卡片显示:名称、图标、已装版本数、当前版本;
- 点卡片进入工具详情:左侧版本列表(实时解析镜像目录,本地缓存,带"刷新"按钮),每行显示 版本号 / 大小 / 可用镜像源 / [安装] 按钮;
- 每个版本可展开选择源(默认按源优先级自动选);
- 目录为**内置声明式清单**(JSON,随应用打包 + 可被 AppData 下用户配置覆盖),不允许用户从 UI 任意添加 URL(防止变成下载器滥用面,后续版本再开放)。

### 4.2 下载(全局组件)
- 顶栏下载图标 + 角标;点击弹出队列面板:每个任务显示 进度条 / 速率 / 剩余时间 / [取消];
- 实现:HTTP Range 断点续传(网络中断恢复后从断点续,不重头);并发上限 2;
- 完成后自动进入安装(校验 SHA-256 → 解压 → 登记),任务行变为"✓ 已安装";失败显示可理解的错误(哪个源、什么错)+ [重试] [换源]。

### 4.3 页面「已安装」
- 按工具分组表格:版本 / 路径 / 大小 / 来源 / 安装时间 / 状态(当前●);
- 行操作:**[设为当前]**(重建 Junction,秒级,弹一行 toast"已切换,新开的终端立即生效")、**[打开目录]**、**[卸载]**(预览将删除的文件与环境改动 → 确认);
- 顶部每工具一个搜索框过滤版本(MVP 不做复杂筛选)。

### 4.4 页面「环境」
- **本工具管理区**:固定 3~4 条 PATH 条目 + `JAVA_HOME` 的当前值,每条显示"✓ 正常 / ⚠ 失效";
- **外部条目区**:遍历用户/系统 PATH 中非 DevRoot 条目,标红指向不存在目录的项,列出"看起来可疑的重复项";提供 [清理所选](默认不勾选任何项);
- **状态备份**:显示最近 N 次环境变量快照(时间+键+旧值),每条带 [恢复此状态];
- 底部按钮 [重新体检]:重扫并给出汇总("PATH 共 23 条,失效 2,重复 1")。

### 4.5 页面「历史」
- 时间线:安装 / 切换 / 卸载 / 环境变更 / 恢复 全部操作,含耗时与结果;
- 环境变更类记录直接链接到对应备份([回滚]);
- MVP 只做展示 + 过滤(按类型),不做复杂检索。

### 4.6 页面「设置」
- DevRoot 路径(改动仅影响后续安装,不迁移已有);
- 镜像源优先级:上/下拖拽排序(源池:华为云、清华 TUNA、Apache 官方、Adoptium 官方、nodejs.org);
- 网络:代理 URL(留空=跟随系统)、并发数、下载缓存目录与 [清理缓存];
- 关于:版本、目录清单版本、检查更新按钮(MVP 手动,不做自动更新)。

## 5. 首批工具与镜像规则(内置目录,已按 M0 实测定稿)

> 下表为 M0(2026-09-07)实测结果,已固化为 `catalog/node.json`、`catalog/jdk.json`、`catalog/maven.json`;完整证据(解析数、下载字节、哈希、耗时)见 `m0-results.json`。镜像改版时按同流程重新探测修订。

| 工具 | 包形态 | 首选源 | 回退源 | 校验和 |
|------|--------|--------|--------|--------|
| Node.js | win-x64 `.zip` | 华为云 `mirrors.huaweicloud.com/nodejs/vX.Y.Z/node-vX.Y.Z-win-x64.zip`(实测 862 版) | 清华 `…/nodejs-release/`(792 版);nodejs.org 官方(实测直连可达) | 与包同目录 `SHASUMS256.txt`(v1.1 所写 `SHASUMS256.txt.txt` 系笔误) |
| JDK(Temurin) | `.zip` | 版本发现走 **Adoptium API**(`api.adoptium.net`,境内可达、自带 checksum);二进制走 **USTC** `mirrors.ustc.edu.cn/github-release/adoptium/temurin{N}-binaries/LatestRelease/`(实测 4MB/s,**仅保留各 major 最新构建**) | 历史版本经 `ghfast.top` 代理 → GitHub Release(实测 sidecar 一致 + Range 206);GitHub 直连本机不可达(仅海外回退);**产品级备选发行版:Amazon Corretto**(corretto.aws 实测直连 206,v1.x 目录扩充候选) | API 内 `package.checksum`(权威);资产 `.sha256.txt` 侧车(USTC 与代理两处实测与 API 一致) |
| Maven | `bin.zip` | 华为云 `…/apache/maven/maven-3/3.9.x/binaries/apache-maven-3.9.x-bin.zip`(实测 37 个历史版,rc 需过滤) | 清华 `…/apache/maven/`(仅当前版);archive.apache.org(全历史,境外) | **Apache 官方 `.sha512`**(dlcdn 当前版 / archive 全史)——镜像目录**不同步**校验文件,v1.1 假设的"同目录 .sha256"不成立;已实测镜像字节与官方一致 |

注意:HTML 目录页解析规则(实测正则 `dirRegex`/`fileRegex`)固化在 catalog JSON;版本发现 = 拉目录页 → 正则提取 → 过滤平台后缀。**JDK 例外**:目录源为 Adoptium API JSON(catalog `listKind: "adoptiumApi"`),Temurin 资产名三种格式并存(`8u504b01`/`11.0.32.1_1`/`21.0.9_10`),版本一律以 API `release_name` 为准。

## 6. 环境变量策略(核心设计)

```
D:\dev\                          ← DevRoot
├── tools\
│   ├── node\22.17.1\            ← 真实文件,多版本并存
│   ├── node\20.19.0\
│   ├── jdk\temurin-21.0.4\
│   └── maven\3.9.9\
├── current\                     ← Junction 层(切换 = 改指向,普通用户可建,无需管理员)
│   ├── node  → ..\tools\node\22.17.1
│   ├── jdk   → ..\tools\jdk\temurin-21.0.4
│   └── maven → ..\tools\maven\3.9.9
└── cache\                       ← 下载缓存
```

用户 PATH 只追加 3 条终身固定项:
```
%DevRoot%\current\node
%DevRoot%\current\maven\bin
%JAVA_HOME%\bin            (JAVA_HOME = %DevRoot%\current\jdk,用户级变量)
```
切换版本 = 重建一个 Junction,PATH 一个字符都不变。新终端立即生效(写注册表后广播 `WM_SETTINGCHANGE`)。

**写入协议**:读取 `HKCU\Environment` 全量 → 存快照(JSON,按时间戳,保留最近 20 份)→ 写新值(`REG_SZ`/`REG_EXPAND_SZ` 正确区分)→ 广播 → 记录操作日志。任一步失败:还原快照。

## 7. 技术方案(决策)【v1.1 修订:已改选 Electron + Vue + 全栈 TS,见《技术手册.md》§1;以下原 Tauri 评估仅留档】

| 层 | 选型 | 理由 |
|----|------|------|
| 壳 + 核心 | **Tauri 2**(Rust) | 安装包 ~10MB、内存占用低;下载/解压/注册表/Junction 在 Rust 侧一次写清;系统能力调用是它的强项 |
| 前端 | **Vue 3 + TypeScript + Vite + Naive UI** | 复用你的 Node/前端经验;表格/抽屉/进度类组件 Naive UI 现成且轻 |
| 通信 | Tauri `invoke`(命令)+ Event(下载进度流) | 无需自建 HTTP 服务 |
| 关键 crate | `tokio`、`reqwest`(流式+Range)、`sha2`、`zip`、`rusqlite`、`winreg`、`windows`(Junction/广播) | 实现期可替换同级别名 |
| 存储 | SQLite(`%APPDATA%\DevKit\devkit.db`)+ 环境变量快照 JSON 文件 | 见 §8 |
| 打包 | Tauri 内置 NSIS 安装包 | 免装 WebView2 检测逻辑走 Tauri 自带 |

**被否方案**:Electron(150MB+ 只为一个管理面板不值);.NET(放弃前端复用);Python+Qt(打包与界面表现力都弱)。理由备查,若 M0 验证发现 Tauri 在目标机受限(如无法装 WebView2)再重评。

### 架构分层
```
[Vue 前端] --invoke/event--> [Tauri commands] --> [core: catalog | download | install | env(junction+registry) | store | history]
```
core 全部为纯 Rust 模块、不依赖 Tauri —— 便于脱离 GUI 跑单元/集成测试(CLAUDE.md 验证约定的落点)。

## 8. 数据模型(存储介质 v1.1 修订:SQLite → JSON repository,见技术手册 §9;字段模型不变)

- `installs(id, tool, version, path, source_url, sha256, size, installed_at, is_current)`
- `operations(id, ts, kind[install|uninstall|switch|env_write|env_restore|download], detail_json, ok, duration_ms)`
- `catalog_cache(tool, fetched_at, json)` — 版本列表缓存
- 环境快照独立 JSON:`%APPDATA%\DevKit\env_backups\<ts>.json`(与数据库解耦,删库也能救)

## 9. 里程碑计划

| 里程碑 | 内容 | 验收标准(需自动化测试通过后方可交付检查) |
|--------|------|------|
| **M0 镜像探测**(先于一切代码) | 脚本实测 §5 全部 URL 规则与目录页解析;失败项确定回退链 | 三工具的 目录解析→真实下载→SHA256 匹配 全通过,清单 JSON 定稿 |
| **M1 core(TypeScript)** | catalog/download/install/env/store/history 六模块(纯 TS,零 Electron 依赖);Junction+HKCU 写入+备份回滚 | 单元:URL 规则、清单解析、备份回滚逻辑;集成:临时目录下真实"下载→解压→建链→读 PATH"与注册表读写测试(HKCU 测完还原) |
| **M2 GUI 骨架** | Tauri 壳 + 4 页面接通 M1 命令;首跑向导 | Node 工具全链路手动走查通过;界面操作无未处理异常 |
| **M3 MVP 完整** | 三工具齐(JDK/Maven)、环境页体检/回滚、历史页、设置页 | §10 DoD 全清单逐条勾验 |
| **M4 发布** | NSIS 安装包、使用说明、README | 在"干净"用户目录跑一遍首装→使用→全卸载无残留 |

> 每个里程碑完成即 `git commit` 打 tag(遵循 CLAUDE.md 变更前回退点约定);测试不绿不交付。

## 10. MVP 完成定义(DoD)

- [ ] 首跑向导:选目录→写 PATH→完成,≤ 60 秒
- [ ] 商店页能装 Node/JDK/Maven 任意版本,断网恢复能续传
- [ ] 校验和错误的包被拒绝安装并明确提示
- [ ] 切换版本 ≤ 1 秒,新开终端 `node -v`/`java -version`/`mvn -v` 正确
- [ ] 卸载后 `tools\` 与 `current\` 无残留、记录无此项
- [ ] 环境页能看到全部历史快照并可回滚
- [ ] 体检能标红人工制造的失效 PATH 项
- [ ] 安装包体积 ≤ 120MB(Electron 现实值,见技术手册 §1 代价声明),启动 < 2 秒

## 11. 后续版本路线图(不承诺时间)

- v1.1:Python(uv 发行方案或直接接管安装器)、下载多源自动测速、manifest 导出/导入
- v1.2:MySQL(zip 版 + 初始化向导)、Profile 环境方案、项目级切换(依赖 shell 集成调研)
- v1.x+:自动更新、私有镜像配置、软件目录开放编辑、签名发布

## 12. 风险登记

| 风险 | 应对 |
|------|------|
| Temurin 在清华镜像的托管路径与假设不符 | **已发生并已解(M0 2026-09-07)**:清华 github-release 无 adoptium(404)、USTC 无历史 tag(仅最新)、GitHub 直连不可达 → 改为 Adoptium API(发现)+ USTC LatestRelease(最新版高速下载)+ ghfast.top 代理(历史版)三层链,见 §5 |
| Temurin 历史版本依赖第三方 GitHub 加速器(ghfast.top),可能失效 | 代理前缀做成 catalog/设置可配置项;失效时备选其它 ghproxy 或引导用户走 Corretto 目录(v1.x 扩充);M1 下载器按"源链顺序回退"设计天然覆盖 |
| 镜像目录页改版 | 解析规则在清单 JSON,配置级修复;版本缓存兜底(可离线用已装源) |
| Junction 广播生效失败(终端不刷新) | 文档说明"重开终端";测试覆盖 SendMessageTimeoutW |
| Electron 体积/内存偏大(v1.1 选型已知代价) | 用时才开、非常驻;builder 裁 locale + lzma 压缩;硬伤时按技术手册 §14 迁移 Tauri(core 隔离即迁移缝) |
| 注册表写坏用户 PATH | §6 备份协议 + 恢复 UI 常驻 + 集成测试最后一步强制还原 |

## 13. 待定项

- 产品正式名称与图标;
- 是否开源(影响签名/更新策略);
- JDK 是否同时收录国产发行版(毕昇/Dragonwell)作为目录运营点。
