# DevKit — 技术手册

> 版本:v1.9 日期:2026-09-24(v1.9:F6-impl 回写 —— §6 新增 `listScan` array 增强:`root`(对象内嵌数组)/`assetNamePath`(url 末段)/pick+fileRegex 过滤 /哈希长度分派 128hex→sha512,四份新 catalog 的源定稿(hmeter/ant/tomcat officialSidecar · dotnet discoveredInline sha512)+ **Tomcat 两级扫描前提作废**、§13 F6-impl 行 + §14 决策记录 2026-09-24;v1.8:C3 回写 —— §7.2 增「◇C3 动态受管计划」(envPlanForInstalls/toolManagedContribution/ctx.env 接线,EnvPlan.javaHome 改可空,removeJavaHome 回收口径)+ §13 C3 行 + §14 决策记录 2026-09-21;v1.7:C 轮回写 —— §7.1.1 系统级口径收窄(★C2 方向 A:HKLM 唯一新增写出口 = applySystemPathAdd"向系统 PATH 追加一条",F3 的自定义变量增/改/删整段下线;readSystemVars 改 fail-closed)+ §8 通道置换(env:system-set/remove 下线,新增 env:system-path-add)+ §13 C 轮行;v1.6:F5 回写 —— §6 新增 `listScan.shape: array`(Go dl API 数组形态 + pick/versionRegex)、`checksum.kind` += `discoveredInline`(API 内嵌哈希)、`listKind` += `regexPage`(官方发布页数据行扫描,sqlite.org 形态)与"华为云 SPA 改版"实测警告、§13 新增 F5 行;v1.5:F1/F2/F3 功能轮回写 —— §6 新增 `adopt` 清单段(接管已有安装,四型探测)、§7.1 新增系统级(HKLM)读写与保护名单、§8 新增 6 个通道、§4 的 core 模块清单不变(adopt 落在 install.ts)、§13 新增 F 轮行;v1.4:M4 交付回写 —— §10 NSIS/瘦身/依赖分区/镜像出包细则(含"locales 裁剪走 afterPack 非 electronDist"实测修订)、§13 M4 行标配置完成待走查;v1.3:M3 交付回写 —— §13 M3 行标完成待走查、§7.6 新增 undici 代理/fileUrlFor 前缀/preload 漏参三条实测;v1.2:M2 交付回写 —— 新增 §7.6 开发工具链实测、§13 M2 行标完成;v1.1:M0 实测回写 §6 源定稿与校验和三类型、§13 M0 行标完成)
> 定位:指导编码的技术细则。产品"做什么"见《产品文档.md》,本文回答"用什么、怎么写、怎么测"。
> **本手册的技术选型自修订日起生效,取代产品文档 §7/§8 原 Tauri 决策**(产品文档已同步标注)。

## 1. 选型结论:Electron + Vue 3 + TypeScript(全栈单语言)

| 候选 | 判定 | 一句话理由 |
|------|:---:|-----------|
| **Electron + Vue + TS** | ✅ **采用** | 唯一零新语言的方案:界面、逻辑、系统调用、测试脚本全是 TypeScript;下载/解压/注册表/Junction 全部有 Node 标准库或 5 行 shell 互操作可解;"易实现"权重最高 |
| Tauri 2(Rust core) | ❌ 降级为备选 | 体积/内存更优,但引入 Rust 学习曲线,与"先做出来"冲突。**若未来包体或性能成为硬伤再迁移**,UI 层代码(Vue)可整体保留 |
| .NET / Go / Python | ❌ | 都是新语言或新 UI 技能栈,不满足"我们已有 Node/前端经验"这一最大杠杆 |

**明确接受的代价**(写下来,免得将来后悔):安装包约 90~120MB、常驻内存 150~250MB、better 原生模块尽量回避。缓解:本工具是"用时才开"的面板类软件,非常驻;体积问题通过 NSIS 压缩 + 不打包多余 locale 缓解(见 §10)。

## 2. 开发环境要求

| 项 | 要求 |
|----|------|
| Node.js | LTS ≥ 20(建议 22.x),用 `nvm-windows` 或 **本工具自己装**(dogfooding) |
| 包管理 | pnpm ≥ 9(仓库锁 `packageManager` 字段) |
| 脚手架 | **electron-vite**(`pnpm create @quick-start/electron devkit --template vue-ts`) |
| 编辑器 | VS Code:ESLint/Prettier(或 Biome)、vue-tsc |
| 权限 | 日常开发**不需要管理员**(HKCU + junction 均为用户级能力)——这是测试前提,任何功能不得以"需要管理员"为设计借口 |
| git | 每个里程碑结束打 tag(`v0.1-m0` 等);任何开发任务开始前先 commit(CLAUDE.md 规范) |
| 工程根 | **脚手架直接建在仓库根 `C:\save_data\demo`**(§4 的 `devkit/` 树即根目录布局,中文文档与代码同级,`.gitignore` 用 electron-vite 模板自带);不在子目录另起炉灶 |
| M0 前置 | `m0-probe.ts` 需要 **Node ≥ 22**(原生类型剥离直跑 .ts,或先 `pnpm dlx tsx`);若本机无 Node:先装 Node LTS(恰好是产品的第一个 dogfood 场景,人工版) |

## 3. 固定依赖清单(超出此表需先讨论)

```
运行时壳        electron ^37, electron-vite, electron-builder
前端            vue ^3.5, vue-router, pinia, naive-ui, @vicons/ionicons5
校验/解析       zod(目录清单 schema)、semver(版本排序)、undici 或 Node 内置 fetch(HTTP)
                → M3 定:HTTP 用 Node 内置 fetch;新增 undici(仅用 setGlobalDispatcher/ProxyAgent/EnvHttpProxyAgent 接代理,§7.6)
解压            extract-zip(MVP 够用;需要进度条时换 yauzl 重写)
存储            electron-store(设置项)+ 自研 JSON repository(业务数据,§9)
日志/工具       electron-log, chokidar(可选:监听 DevRoot 外部改动)
测试            vitest, @vue/test-utils;(M3 可选)playwright + _electron
```

> 刻意不用 better-sqlite3 等**原生模块**(node-gyp/rebuild 是 Electron 第一大坑),数据量(MVP 数千行)用 JSON 文件 + 抽象仓储足够,接口抽象好,将来无痛换 SQLite。

## 4. 工程结构(核心原则:`core/` 禁止 import `electron`)

```
devkit/
├── catalog/                    # 内置工具清单 JSON(§6),随包发布
│   ├── node.json  jdk.json  maven.json
├── resources/                  # 图标、ps1 脚本(env.ps1 §7)
├── src/
│   ├── shared/                 # main↔renderer 共享:类型、IPC 通道常量、zod schema
│   ├── main/
│   │   ├── index.ts            # 壳:窗口、生命周期 —— 越薄越好
│   │   ├── ipc.ts              # ipcMain.handle 注册表(仅参数转发,无逻辑)
│   │   └── core/               # ★ 全部业务与系统逻辑,纯 TS,可被 vitest 直测
│   │       ├── catalog.ts      # 清单加载 / 镜像目录页解析 / 版本列表缓存
│   │       ├── download.ts     # 下载队列:Range 续传、SHA256、取消
│   │       ├── install.ts      # 解压、登记、卸载
│   │       ├── env.ts          # 注册表读写、备份回滚、广播、体检
│   │       ├── junction.ts     # current\ 链接的建/换/删
│   │       ├── history.ts      # 操作日志
│   │       └── paths.ts        # DevRoot 布局唯一权威(§5),禁散拼字符串
│   ├── preload/                # contextBridge 白名单(§8)
│   └── renderer/
│       └── src/
│           ├── views/          # Store / Installed / Environment / History / Settings / Setup 向导
│           ├── stores/         # pinia:downloadQueue、catalogCache…
│           └── api.ts          # window.devkit 的类型化包装,组件只碰这层
├── scripts/
│   ├── m0-probe.ts             # ★ M0 镜像探测脚本(第一个可运行物,§11)
│   └── env-selftest.ps1        # 注册表/广播链路自检
└── tests/                      # vitest projects:core(node 环境)+ renderer(happy-dom)
```

> **铁律**:任何人往 `core/` 里 import `electron`/`BrowserWindow` = 评审打回。壳与逻辑分离是"可测试"的唯一保障,也是将来若换 Tauri 时唯一的迁移缝。

## 5. 磁盘布局与 DevRoot 规约(与产品文档 §6 一致)

```
<DevRoot>\tools\<tool>\<version>\   # 真实文件,永不互相引用
<DevRoot>\current\<tool>            # junction → tools\<tool>\<当前版本>
<DevRoot>\cache\*.part *.part.json  # 下载中断点状态
```
- 默认 DevRoot:`D:\dev` 存在 D 盘则用,否则 `%USERPROFILE%\dev`;创建时校验:**纯 ASCII、无空格、非同步盘目录**(OneDrive 路径要警告);
- 一切路径拼接只许走 `core/paths.ts`,禁止手写 `\` 字符串。

## 6. 工具清单(catalog JSON)格式

```jsonc
// catalog/node.json —— 一个工具一份;zod 校验;这是"配置即数据",镜像改版改这里
{
  "id": "node",                       // 内部标识,同时是 tools\ 目录名
  "displayName": "Node.js",
  "platformRegex": "^v(?<ver>\\d+\\.\\d+\\.\\d+)/node-v\\k<ver>-win-x64\\.zip$",
  "sources": [                        // 按序即优先级;settings 可覆盖
    { "id": "huawei", "listUrl": "https://mirrors.huaweicloud.com/nodejs/",
      "fileUrl": "https://mirrors.huaweicloud.com/nodejs/{path}" },
    { "id": "official", "listUrl": "https://nodejs.org/dist/",
      "fileUrl": "https://nodejs.org/dist/{path}", "proxyable": true }
  ],
  "checksum": { "kind": "shasumsFile", "url": "https://mirrors.huaweicloud.com/nodejs/{dir}/SHASUMS256.txt" },
  "rootDir": "node-v{ver}-win-x64",   // zip 内顶层目录(决定解压后如何归一化)
  "layout": "binAtRoot"               // binAtRoot(node.exe 在根)| binSubdir(maven 有 bin\)
}
```
- 目录页解析:拉 `listUrl` → `platformRegex` 扫 `<a href>` → semver 排序 → 缓存于 `catalog_cache.json`(TTL 24h,UI 强制刷新绕过);
- **JDK/Temurin 源已由 M0 实测定稿(2026-09-07)**,目录清单新增 `listKind` 两型:`dirIndex`(HTML 目录页,Node/Maven)与 `adoptiumApi`(Temurin:Adoptium API 做版本发现,二进制源链 USTC LatestRelease → ghfast.top 代理 GitHub → GitHub 直连);本文件 §6 示例仅示格式,**一律以 `catalog/` 实测定稿文件为准**;
- **★F4(2026-09-16,±九份清单)新增能力**:
  - `listKind` 再增两型:`jsonApi`(版本 API,JetBrains 键控对象走 `listScan` 点路径,资产/校验 URL 随发现携带)+ `latestRedirect`(无索引、只给最新的官方重定向,如 VS Code `/latest/win32-x64-archive/stable` 302 后从终地址文件名提版本);
  - 非 semver 版本:`rawVersion:true` 保留原文(USTC github-release 的 git tag `2.55.0.windows.5`),排序用自然序(`naturalCompare`:数字段按数值);
  - `aliases` 模板别名:`{gitver}` = `{ver}` 去掉 `.windows.`(MinGit 资产名 `MinGit-2.55.0.5-64-bit.zip`);
  - `maxVersions` 截断最新 N 个(JetBrains 263 条/JetBrains 版本列表不刷屏);
  - `versionPolicy.exclude` 显式排除占位版(python.org 的 `3.15.0/` 目录被 3.15.0a1 α 占用、无正式 embed 包);
  - 校验与解压范式:`checksum.kind` 增 `discoveredSidecar`(JetBrains API 的 checksumLink,裸哈希)与 **`pinnedHash`**(官方不发布 sidecar 的工具:内置「版本→哈希」表,发版例行更新,**下载仍永远校验**;表外版本抛 `checksum-unpinned` 拒装);`layout: binSubdir` 可配 `binName`(MinGit 的 git.exe 在 `cmd\`);空 `rootDir` = 平铺根(python embed / MinGit / VS Code 新 CDN 扁平 zip);
  - 真实 e2e 与校验背书:python 四版 pinned 与 python.org 官方全包 sha256 逐一比对一致;dbeaver/vscode/git 真下载安装通过,adopt 探测连锁命中(git `cmd\git.exe --version`、vscode `bin\code.cmd --version`、python `python.exe --version`、dbeaver `dbeaver.exe --version`、IDEA/PyCharm 走 `build.txt` 的 IC-/PC- build)。复验命令:`scripts/f4-e2e.mts --discover / --pin <id> / --install <id>`(esbuild 打包后 node 运行)。
- **★F5(2026-09-19,+三份清单:gradle/go/sqlite)新增能力与实测警告**:
  - `listScan.shape` 增 `array`(Go dl API 形态:响应是版本对象**平数组**,每版本内 `pick` 键过滤目标资产——`{os:'windows',arch:'amd64',kind:'archive'}` 全命中才取,`versionRegex` 首捕获组规整版本串——剥 `go` 前缀,资产文件名/内嵌 sha256/体积随发现携带);
  - `checksum.kind` 增 `discoveredInline`(校验权威 = 版本发现时 API 响应内嵌的哈希,`adoptiumApi` 同范式;Go 官方 dl API 每文件自带 sha256,权威、零额外请求、天然跨域);
  - `listKind` 增 `regexPage`(**官方发布页数据行扫描**,sqlite.org download.html 形态:常规 `<a>` href 是 JS 注入的占位,真实数据在 HTML 注释内的 `PRODUCT,<ver>,<path>,<size>,<sha3>` CSV 行——`dirRegex` 在此语义下是**行正则**(逐 matchAll 扫全页,命名组 ver/path/size),`{path}` 组须把 `.zip` 吸进去、fileUrl 模板直拼 `https://www.sqlite.org/{path}`;资产名 = path 末段过 `fileRegex` 校验);
  - **⚠ 华为云镜像 SPA 改版实测警告(2026-09-19)**:`mirrors.huaweicloud.com` 已改单页应用,`/cmake/`、`/sqlite/`、`/nginx/download/` 等目录**名存实亡**——任意文件 URL 返回 HTTP 200 的 ~12KB HTML 兜底页(非文件)。收录新源必须用 **ZIP PK 魔数或 Range 206 + content-length** 验真,不能只看状态码(gradle `/gradle/`、apache `/apache/`、golang 之外的目录仍真);华为云 `/golang/` 401(热链拦截);
  - 三工具源定稿:gradle=华为云/腾讯云 dirIndex(各 170 版直链)+ 官方 `services.gradle.org/.sha256` 裸哈希侧车(华为云同名 sidecar 与官方抽验一致,S1 跨域由 orderChecksumUrls 保障);go=`golang.google.cn/dl/?mode=json&include=all`(365 版带 win-amd64 sha256+size,zip 206、1.6MB/s)+ 阿里云 171 版兜底;sqlite=官方 download.html 单源(国内无镜像)+ `pinnedHash`(官方只有 SHA3-256 汇总页非 SHA-256;3.53.4 真哈希 `f46ee247…429f9fc` 已回填,发新版例行 `--pin sqlite` 回填);
  - 真机 e2e:三工具全过 —— gradle 9.7.1(151MB,官方侧车校验,lib\gradle-core-9.7.1.jar 探测;`gradle.bat --version` 实测需 JAVA_HOME 属工具常识)、go 1.27.1(75MB,内嵌 sha256 校验,VERSION 文件探测,`go version go1.27.1 windows/amd64` 实测)、sqlite 3.53.4(6MB,pinned 校验,平铺根,`sqlite3 --version` 实测)。
- **★F6-impl(2026-09-24,+四份清单:jmeter/ant/tomcat/dotnet)新增能力与源定稿**:
  - `listScan.shape: array` 增强(**.backward-compatible**):①`root` 点路径取**对象内嵌数组**(.NET releases.json 的 `{releases:[…]}`,Go 平数组时缺省行为不变);②`assetNamePath` 缺省 `filename`,.NET 取 `url`(完整地址)并抽取**末段文件名** `dotnet-sdk-10.0.401-win-x64.zip`;③pick 命中后再用 **fileRegex 过滤** —— .NET 同 rid 下 `.exe` 与 `.zip` 并存(如 win-x64),pick 只筛平台、fileRegex 挑 zip,exe 被跳过;④数组发现的内嵌哈希按**长度分派算法**:64hex=sha256(Go)、128hex=sha512(.NET releases.json 的 `hash` 字段);
  - **Tomcat「两级扫描」归档前提作废(fact)**:dirIndex 的"版本目录"语义天然覆盖 —— `tomcat-11/` 下 `v11.0.26/` 即版本目录(同 Maven 的 `3.9.9/`),fileUrl 模板静态拼 `…/tomcat-11/v{ver}/bin/apache-tomcat-{ver}-windows-x64.zip` 即可,无需 catalog.ts 新增任何两级扫描能力;
  - 四工具源定稿:jmeter/ant/tomcat = **officialSidecar sha512**(镜像=华为云(+USTC/清华)apache 目录实测真 + PK 魔数;镜像**不同步** .sha512 → 校验走官方 `archive.apache.org` 跨域,同 Maven/S1);dotnet = **jsonApi array + discoveredInline sha512**(官方 `builds.dotnet.microsoft.com/dotnet/release-metadata/{channel}/releases.json` 内嵌 128hex SHA-512,**官方单源无国内镜像** —— 镜像优先支柱例外,评估已记;包 200MB);
  - 真机 e2e:四工具版本发现全过(jmeter 16/ant 22/tomcat 24/dotnet 13 版);ant 1.10.18 / jmeter 5.6.3 / tomcat 11.0.26 完整 install() 闭环(下载→sha512→解压→登记→junction);dotnet(200MB)仅验证发现/URL/校验链路,完整安装留走查;e2e 脚本 `scripts/f6-e2e.mts`(--discover / --url &lt;tool&gt; &lt;ver&gt;)。
- 校验和实测共三种 `checksum.kind`:`shasumsFile`(Node,SHA-256)、`adoptiumApi`(Temurin,SHA-256,API 响应自带、权威)、`officialSidecar`(Maven,**SHA-512**,只能取 Apache 官方源——镜像目录不带校验文件)。`core/download.ts` 哈希须同时支持 sha256/sha512;
- **`shasumsFile` 行格式铁律(M2 走查实测,曾致 bug)**:Node 的 `SHASUMS256.txt` 每行是 `<64位hex>` + **恰好两个空格** + 文件名(`hashsum -b` 标准输出)。catalog `lineMatch` 里那两前导空格**只是书写提示**——core 匹配时哈希↔文件名的间距一律由正则 `\s+` 吸收,`lineMatch` 前导空白须先 `trimStart` 再入正则,否则等价于要求 ≥3 空白、永不匹配(表现:所有 node 版本"取不到校验和",而 jdk/maven 走别分支不受影响)。§11 已补该分支回归测试;
- 解析出的 version 记录 `sourceId + path`,下载/校验共用。

### 6.1 `adopt` 段(★F1 接管已有安装;2026-09-14)
把"本机已装好的目录"纳入管理,**零 core 改动扩新工具**:加工具只做两件事 —— 写一份 catalog JSON、在里面补 `adopt`。

```jsonc
"adopt": {
  "markers": ["bin\\java.exe"],       // 全部存在才认定"这个目录是该工具"(防选错)
  "exec": "bin\\java.exe",            // 展示用主可执行(可省)
  "binDir": "bin",                    // 该工具 PATH 需要的子路径(文档/后续版本用)
  "version": [                        // 版本探测链:按序试,首个命中者胜出
    { "kind": "releaseFile", "file": "release", "regex": "JAVA_VERSION=\"([^\"]+)\"" },
    { "kind": "dirName",     "regex": "jdk-?(\\d+(?:\\.\\d+)*)" },
    { "kind": "exec",        "exe": "bin\\java.exe", "args": ["-version"], "regex": "version \"([^\"]+)\"" }
  ]
}
```
- 四型探测:`releaseFile`(读文件+正则)/ `fileGlob`(列目录取首个匹配名)/ `dirName`(目录名正则)/ `exec`
  (跑树内 exe,`.cmd/.bat` 经 `cmd.exe /c`,stdout+stderr 合并);一律取**第 1 个捕获组**,命中后去首尾空白
  与去前导 `v`。版本串只作标签与登记键,**不参与路径推导**(接管项目录从不按版本推导)。
- 已定稿:`jdk`=releaseFile→dirName→exec;`node`=exec(`node -v`)→dirName;`maven`=fileGlob(`lib/maven-core-<ver>.jar`)→dirName→exec。
- 接管登记 `origin:'adopt'`:`path` 在 DevRoot 之外、`sourceId:'local'`、`sha256:''`、`size:0`(不统计体积);
  该工具首个版本自动建 `current` 链。**只登记 + 建链,不复制/不移动/不删除任何文件**(`forgetInstall` 只删登记行 + 断链)。
- 接管前置校验六拒(全部在 `install.ts:assertAdoptableDir`,错误码可直接展示):`adopt-unsupported`(无 adopt 段)/
  `adopt-bad-path`(空·相对)/ `adopt-unc`(UNC)/ `adopt-not-dir` / `adopt-is-link`(要真实目录)/ `adopt-inside-devroot` / `adopt-not-tool`(缺标记)。
- `uninstall` 对 `origin:'adopt'` 抛 `adopt-unregister-only`(删除入口永不指向别人的目录,§7.3 红线的自然延伸);
  `resolveOpenableInstallDir`(S2 收口)对接管项把"必须在 DevRoot 内"换成"本机绝对路径 + 非 UNC"——
  **路径仍全部出自主进程登记表**,收口语义不变。

## 7. 系统层细则(全项目最需要小心的 60 行)

### 7.1 注册表读写 —— 经 PowerShell,不引原生模块
`resources/env.ps1` 暴露三个函数,Node 侧 `execFile('powershell.exe', ['-NoProfile','-NonInteractive','-EncodedCommand', b64])`,**参数一律走 EncodedCommand**(UTF-16LE base64),杜绝引号注入:

> ⚠ 维护陷阱(M1 实测):仓库内所有 `.ps1` **必须存为带 BOM 的 UTF-8**——Windows PowerShell 5.1 对无 BOM 文件按 GBK 误读,含中文即炸语法。新建/转换后用 `scripts/env-selftest.ps1` 验证。

- `Get-Env` → 读 `HKCU:\Environment` 返回 JSON;
- `Set-Env -Name Path -Value ... -Kind ExpandString` → `New-ItemProperty`/`Set-ItemProperty`,**必须显式 `-Type ExpandString`**,否则 `%USERPROFILE%` 会被打平;
- 写完后 `SendMessageTimeoutW(HWND_BROADCAST, WM_SETTINGCHANGE, "Environment")` —— 用 `Add-Type` 内联 P/Invoke 声明(脚本内含一份 8 行 C#)。
  禁用 `setx`:PATH 超 1024 字符会**截断**,一票否决。

#### 7.1.1 系统级(HKLM,★F3 2026-09-14 → ★C2 方向 A 收窄 2026-09-20)
- PS 函数仍在:`Get-SystemEnv` / `Set-SystemEnv` / `Remove-SystemEnv` / `Get-Elevated`,键固定
  `HKLM\SYSTEM\CurrentControlSet\Control\Session Manager\Environment`;**读不需要管理员,写需要**。
- **★C2(用户拍板)写出口收窄为一**:`EnvService.applySystemPathAdd(entry)` —— **向系统 PATH 追加一条**,
  仅此而已;F3 时代的"自定义变量增/改/删"(`applySystemVarSet/Remove`、`env:system-set/remove` 通道、
  环境页全变量表)整段下线。**不提供删除与修改**(用户口径:到 Windows 系统设置手动做)。
  条目校验 `assertSystemPathEntry`:trim 非空、禁 `;` `"` 换行、须盘符绝对路径(`X:\…`,裸根 `C:\` 拒)
  或 `%VAR%` 前缀引用;等值已存在 → 幂等零写入(`mergePathEntries` 只增不删,其余条目原样保序)。
- 闸门与保护仍在 **core**(`env.ts`):①`allowSystem` 回调默认 `() => false`(壳层注入
  `settings.allowSystemEnv`),关着时 `system-write-disabled`,**一次 PowerShell 都不发**;
  ②`PROTECTED_SYSTEM_VARS`(34 个内置名含 `path`)现在只守一处:`restoreTo` 系统回滚**永不删除内置变量**
  (快照缺项也不抹 —— 比"忠实回滚"更重要);Path 的"追加"是设计内写口(经 merge 只增不减),不算绕过。
- **★安全复查轮(2026-09-20)fail-closed 铁律(勿回退)**:`readSystemVars()` 输出不可解析 → 抛
  `system-read`,**不再降级空表**;`applySystemPathAdd` 另拒零值空表(`system-path-read`)——
  该方法的写语义是"读出全量→merge→**整体覆写回 Path 值**",若把"读坏了"当"真的空",
  就会用单条新值覆写整个系统 PATH 且快照同为空(回滚失真)。展示读取走 `readSystemPath()`
  (null 降级语义保留),两口径勿混。
- 写入复用 §7.2 四步(快照→写→广播→失败还原),快照带 `scope:'system'`(老备份无 scope 按 user);
  回滚按快照 scope 选 hive。未提权被系统拒绝 → `system-need-admin`(附中文原因),不把 PS 堆栈甩给用户;
  UI 用 `Get-Elevated` 提前显示管理员徽标。

### 7.2 写入协议(不可绕过的四步)
```
① 全量快照 → userData\env_backups\<时间戳>.json(含 Name/Kind/Value)
② 计算新值(paths.ts 生成,幂等:重复安装不产生重复条目)
③ Set-Env 写入 → 广播
④ 任一步抛错 → 用 ① 还原并 rethrow;成功 → 记 history
```

### 7.2.1 ◇C3 动态受管计划(装什么管什么,2026-09-21 用户新提;推翻 M3 决策 A"固定 3 条")
- **概念**:受管 PATH 条目不再写死 3 条,而是「当前在管工具」(installs 登记里有该工具,无论哪个版本为
  current)各一条;安装/接管成功**自动接入**,卸载/移出登记该工具**清空后自动断开**;切换版本依旧零 PATH
  变动(junction 不变)。
- **唯一权威 = `install.ts`** 三函数(依赖方向:install→paths+catalog+env,无环;`paths.envPlan` 已删,
  别再去代码里找固定三条):
  - `pathEntrySuffixOf(entry)`:`layout:binSubdir` → `\<binName|bin>`(MinGit 是 `cmd`);`binAtRoot` → 空;
  - `toolManagedContribution(devRoot, entry)`:单工具贡献 —— `jdk` 特例 `{ paths:['%JAVA_HOME%\\bin'],
    javaHome: current\jdk }`;**其余** `{ paths:[current\<tool>+suffix], javaHome:null }`。
    收录新工具自动获得 PATH 入口(按 layout/binName 派生,零额外配置);JDK 形态工具在 jdk 特判里扩展;
  - `envPlanForInstalls(devRoot, installs, entries)`:`installs` 的 tool 集合 × 全部 catalog 声明序去重 →
    `EnvPlan`,UI/向导/prune 保护与「增量接入」严格同源。
- **接线**:`InstallContext.env?: EnvService`(壳层注入,测试缺省不写)。`install()`/`adoptInstall()` 登记后
  `applyPlan`(增量,§7.2 四步;接入失败**不回转安装**,记历史 `env_write ok:false`,环境页「重新接入」补齐);
  `uninstall()`/`forgetInstall()` 该工具清空后 `autoDisconnectOnToolEmpty` → `applyRemoval`(精确等值删 PATH
  条目,`%JAVA_HOME%\bin` 是字面匹配)+ `removeJavaHome(expected)`(**只删"现值 == 我们设的 current\jdk"**,
  用户后来另设的不动 —— 红线 §3.3 的变量层延伸)。
- **`EnvPlan.javaHome: string | null`**(v1.8):null = 未在管 JDK,`applyPlan` 不写不删 JAVA_HOME(绝不动用户
  自设变量);环境页 `env:state` 受管区 JAVA_HOME 行仅 jdk 在管时显示(不再悬空)。

### 7.3 Junction —— 零依赖
`fs.symlinkSync(target, linkPath, 'junction')` 即创建目录联接,**普通用户无权限要求**;删除用 `fs.rmdirSync(linkPath)`(只断链)。
**安全红线**:删除 junction 严禁任何递归删除入口(`fs.rm -r`、`rimraf`)先接触链接路径;`install.ts` 的卸载函数第一行必须是 `lstat → isSymbolicLink/junction 判定`。

### 7.4 下载器
- 内置 `fetch`:响应 `Readable` → 写 `*.part`;已存在 part 则带 `Range: bytes=N-`,收到 206 续传、收到 200 清头重下;
- SHA256 边下边 `crypto.createHash` 增量算,续传场景**从 0 重放 part 文件补算**(一次 I/O,内存安全);
- 取消 = 保留 part + 写 `*.part.json`(size/url/etag),重试点亮"续";
- 并发上限 2;网络错误分类(DNS/TLS/超时/403)决定提示文案与 [换源] 可用性。

### 7.5 解压
**PowerShell `Expand-Archive`**(★F4 2026-09-16 起)— 临时目录 → 按 `rootDir` 归一化(空 rootDir=平铺根;拍平唯一顶层)→ `rename` 进 `tools\...`(同盘原子);失败清理临时目录,登记表不写入。
- **为何弃 extract-zip(实测)**:`extract-zip`→yauzl 的 inflate 读流对部分真实 zip **静默截断无 end/error**(python.org embed 的 python.exe 条目只解出 95,994/106,208 字节后卡死,进程静默退出);同一文件 `Expand-Archive` 完整解出 106,208 字节。§7.6 本就约定"解压一律走 Expand-Archive"(fetch-electron 已用),至此安装链路与脚本口径统一。
- 实现:`install.ts:expandZip` —— 复制 part 为临时目录下 `bundle.zip`(`Expand-Archive` 只认 .zip 扩展名)→ `powershell -NoProfile Expand-Archive -Force` → 删 bundle。路径经 PowerShell 单引号转义(DevRoot 可能含空格,§7.1 内涵)。
- 代价:大包解压比 extract-zip 慢(1.5GB 的 IDEA 约数分钟),一次性安装可接受。

### 7.6 开发工具链实测(M2 2026-09-07)
> 均为本机(GitHub 不可直连 + git-bash + Windows 11)实测踩坑,固化以免后续会话重复踩。
- **Electron 二进制**:装 `electron` npm 包时官方 `install.js`(@electron/get + extract-zip)把 ~130MB 包解压会在 `dist/locales` 后**静默停住**(退出码 0 却无 `electron.exe`)。`pnpm install` 只会重跑 JS、不重解 → 已提供 `pnpm bin:electron`(`scripts/fetch-electron.mjs`:镜像下载 + PowerShell `Expand-Archive` 解压)一步补齐;`ELECTRON_MIRROR=https://npmmirror.com/mirrors/electron/`。
- **git-bash 的 `tar` 不可用于 zip**:它是 GNU tar(不支持 zip,且把 `C:\` 冒号误判为远程主机 `host:path`)→ 解压一律走 `Expand-Archive`,勿在脚本里 `tar -xf *.zip`。
- **版本锁 vs 生态**:`electron ^37`(§3 实锁 37.10.3)不随大流跳最新(实探已 44)——§3 规定"超出此表需先讨论";`electron-vite`/`plugin-vue` 无 pin,须与 pnpm 解析出的 `vite` 大版本对齐(vitest 3.2.7 带入 vite@7 → 用 electron-vite@5 / plugin-vue@6,electron-vite@2 会因 peer 仅到 vite5 而不匹配)。
- **不引入 `@electron-toolkit/*`**:§3 未列;preload 直接 `import { contextBridge } from 'electron'` 手搓白名单,主进程窗控亦原生,减依赖面。
- **tsconfig 三段式**:根 `tsconfig.json` 仅 `files:[] + references`;node 项目(main/preload/shared + core 测试,**排除 `tests/renderer`** 否则 .vue/DOM 报错)、web 项目(`vue-tsc`,renderer + shared + `tests/renderer`)。`pnpm typecheck` = 两段串跑。
- **`.ps1` 仍守 §7.1 BOM 铁律**;但 `fetch-electron.mjs` 里对 `Expand-Archive` 的调用是内联 `-Command` 字符串,不入库为 `.ps1`,不受 BOM 约束。
- **代理 URL 接线 = undici 全局 dispatcher**(M3):`setGlobalDispatcher(new ProxyAgent({uri}))` / 留空时 `EnvHttpProxyAgent`(Node 内置 fetch 经同一 `Symbol.for('undici.globalDispatcher.1')` 吃到,Electron 主进程同理)——"留空=跟随系统"的口径实为**跟随 `HTTP(S)_PROXY` 环境变量**(Node fetch 不读 WinINET,UI 文案照此写)。undici 为 §3 表内候选,纯 JS 无 node-gyp。
- **下载 URL 必须走 `catalog.fileUrlFor`,禁手拼 `renderTemplate(src.fileUrl)`**(M3 实测):代理前缀(`proxy.kind='prefix'`,JDK ghproxy 全靠它)在 fileUrlFor 内拼接;M2 及以前 `install.ts` 绕过它 → 带前缀源实际直连 GitHub(本机不可达)。§11 已补"代理前缀真进请求"回归;这是"JDK catalog 接真"的最后一根线。
- **preload 白名单函数务必 `invoke(Channel.X, req)` 带参**(M3 发现的 M2 遗留):`envRestore` 曾写成 `invoke(Channel.EnvRestore)` 漏传 req → env:restore 恒失败但无 UI 触达未被察觉。教训:契约里每个带 req 的方法,冒烟桥/走查至少一路真调到。

## 8. IPC 契约

- preload 只 `contextBridge.exposeInMainWorld('devkit', {...})` 白名单,通道名全部定义在 `shared/ipc.ts`(`enum Channel`),两侧从同一常量 import;
- 请求-响应 `ipcMain.handle`:通道 ⇔ 产品页面对照:`catalog:list / catalog:versions / download:start / download:cancel / install:list / install:switch / install:uninstall / env:state / env:audit / env:prune / env:restore / history:list / settings:get / settings:set / setup:run`;
- **§8 全集之外的新增通道集中登记**(全部定义在 `shared/ipc.ts`,ipc-contract 测试对账):
  M2/M3:`setup:preview / setup:defaults / setup:check / shell:open-path / cache:clear`;
  **F1**:`install:adopt`(接管已有安装)/ `install:forget`(移出登记)/ `dialog:pick-dir`(原生目录选择);
  **F3→★C2**:`env:system-list`(现只回系统 PATH 条目 + 开关/管理员态)/ `env:system-path-add`(追加一条);
  ~~`env:system-set` / `env:system-remove`~~ 已随 C2 下线(契约测试守卫其不复活);
- 推送:main `webContents.send('download:progress', taskId, {received,total,speed})`,renderer 汇进 pinia `downloadQueue`;
- 返回值统一 `{ ok: true, data } | { ok: false, code, message }` 判别联合 —— **异常不许跨进程裸抛**。

## 9. 存储(修订产品文档 §8:SQLite → JSON repository)

`userData\devkit.json` 经 `store.ts` 抽象仓储读写:`installs[] / catalog_cache / settings`;写 = 序列化 → 写 `*.tmp` → `rename` 原子替换;损坏时从 `.bak`(每次写保留上一份)恢复并弹提示。数据模型字段不变(产品文档 §8),仅介质降级为"够用且零依赖";`env_backups\` 与 `history`(JSONL,`userData\history.jsonl` 追加写)独立存放。

## 10. 构建与发布

- `electron-builder`:target **仅 nsis**(`oneClick:false`,`perMachine:false` → per-user,**默认** `%LOCALAPPDATA%\Programs\DevKit`;`allowToChangeInstallationDirectory:true` 装向导中**目录可选**——2026-09-08 用户定,不强制固定位置;**不写 HKLM / 不需管理员**);
- 瘦身:`asar` 开、`compression: maximum`(lzma 实压)、locales 裁剪保 zh-CN/en。**实测修订**:locales 裁剪不走 `electronDist`(那是"自带整个 dist 目录"的开关,不能只删部分),而用 **`afterPack` 钩子**(`scripts/after-pack.mjs` 删 `appOutDir/locales/*.pak` 非保留项,53→2 个 .pak);
- 依赖分区:渲染层依赖(vue / naive-ui / pinia / vue-router / @vicons)归 **devDependencies**(vite 打进 bundle),`dependencies` 只留运行时真需(extract-zip / semver / undici / zod)——否则 builder 会把前者再夹带一份进 app.asar(实测 asar 仅 6.7MB);
- 签名:MVP **不做**(接受 SmartScreen"未知发布者"提示,README 写明处理方式);`win.signAndEditExecutable:false` 免无证书环境跑 signtool/rcedit 链;v1.x 再评估代码签名证书;
- **出包依赖二进制走镜像**(本机 GitHub 不可直连,§7.6):`electron-builder` 拉 electron dist 认 `ELECTRON_MIRROR`、拉 nsis/7za/nsis-resources 认 `ELECTRON_BUILDER_BINARIES_MIRROR` —— 均在 `scripts/dist.mjs` 内置默认值(npmmirror);
- 版本号单一来源 `package.json`;`pnpm dist` = **typecheck + test + build + electron-builder(4 段一个脚本)**,任一段非零退出不出版本(§11 门禁延伸)。

## 11. 测试与质量规范(落实 CLAUDE.md"交前必测")

**分层策略:**
| 层 | 工具 | 强制要求 |
|----|------|---------|
| core 单元/集成 | vitest(node 环境,直接 import `core/*`) | catalog 解析(fixture HTML)、下载 Range(本地 http server)、paths 幂等、写入协议(mock execFile)必须有测试 |
| env 真机链路 | `scripts/env-selftest.ps1` + **沙盒键** | `EnvService` 的键名可注入:自动化测试全部打 `HKCU:\Environment_DevKitTest`(测完全删);真实 PATH 只允许手动走查项覆盖 |
| 安装链路 | vitest + DevRoot fixture(极小 zip 挂本地 server) | "下载→校验→解压→建链→读回"全绿才算 M1 完成 |
| renderer | vitest + @vue/test-utils 冒烟(不追覆盖率) | 组件哑,逻辑都在 pinia,重点测 store |
| 里程碑手动验收 | 产品文档 §10 DoD 清单 | 逐条勾,结果记 commit message |

**门禁**:`pnpm test` 不过 → 不许合并/交付;core 与壳同仓,同一次 commit 必须同测试;改 `env.ts/junction.ts/install.ts` 而测试 diff 为零的 PR,默认打回。

## 12. 编码规范摘要

TypeScript `strict: true` 不许 `any` 逃逸(确需则 `unknown + zod`);ESLint + Prettier(Biome 亦可,团队一人时别纠结);注释与 UI 文案用中文,代码标识符用英文;错误用 `Result` 式判别联合而非到处 throw;时间一律 ISO 字符串。

## 13. 里程碑 → 技术任务映射(产品文档 §9 的落地)

| 里程碑 | 技术产出 | 完成判据 |
|--------|---------|---------|
| **M0**(先行,不依赖脚手架)✅ **已完成 2026-09-07,三工具全绿** | `scripts/m0-probe.ts`:对 §6 三工具的 list/file/checksum URL 实测解析+抽样下载+校验;Temurin 清华路径重点验证 | 达成:catalog JSON 已按实测提交;失败项(清华 adoptium 不存在等)回退源已定并回写产品文档 §5 v1.2 |
| M1 ✅ **已完成 2026-09-07** | `paths/junction/env/download/install/catalog/history` 六模块 + §11 全部测试(实得:另含 store/errors/arch 守卫,共 9 文件) | 达成:`pnpm test` 65/65 绿、`pnpm typecheck` 干净、env-selftest.ps1 真机通过(沙盒键全删);安装链路端到端"下载→校验→解压→建链→读回"以本地 http+真 zip 夹具全绿;tag `v0.2-m1` |
| M2 ✅ **已完成并通过用户手动验收 2026-09-07** | electron-vite 合入根、`shared/ipc` 契约 + preload 白名单、主进程壳(services/ipc/index)、Setup 向导 + 商店 + 已安装三页 + 顶栏下载指示(§4.0/4.1/4.2/4.3) | 达成:typecheck 干净、`pnpm test` 79/79 绿、build 三段产物齐、dev 启动无异常;**用户走查:①Node 下载(修复 §7.6 校验和正则 bug 后通过)②maven junction 切换通过**;tag `v0.3-m2` |
| M3 ✅ **代码完成 2026-09-08(待用户走查)** | 环境/历史/设置三页(§4.4/4.5/4.6)、`env:audit/prune` 补实、体检/回滚/清理 UI、catalog 偏好(源优先级+代理前缀)接真 | 达成:typecheck 干净、`pnpm test` **100/100** 绿(基线 79 + core 16 + 契约 2 + M3 页冒烟 3)、build 三段产物齐、dev 启动无异常。实现要点:`paths.classifyPathEntries`(装配原语,内部只消费 auditPathEntries 未重写)、`env.applyRemoval`(§7.2 四步删除方向)、`env.readSystemPath`(HKLM **只读**,红线 §3.1 写口钉死 CurrentUser)、`ManagedEntryView.targetOk`(决策 A 悬空暴露:present✓ 而 targetOk✗=⚠失效)、prune 壳层守卫拒删受管条目(`prune-protected`)、`cache:clear` 通道、undici 全局 dispatcher 接代理(§7.6);**JDK 接真关键修复**见 §7.6 fileUrlFor 条 |
| M4 ✅ **完成并经用户验收 2026-09-08,tag `v1.0.0`** | electron-builder NSIS(§10 全段:per-user/**目录可选(用户走查时要求,不强制固定位置)**/lzma/asar/`afterPack` locales 裁剪/无签名链)、`scripts/dist.mjs`(4 段一脚本 + npmmirror 内置)、README.md(含 SmartScreen 与装/卸语义) | 达成:`pnpm dist` 全流程绿、`devkit-setup-0.1.0.exe` **81.2 MB**(DoD ≤120MB)、win-unpacked 启动冒烟 4s 存活无异常、asar 仅含 4 运行时依赖+应用码;**用户走查"大体上没问题"通过**,遗留项(用户明示"后续再来"):**bug 修改清单 + 新功能清单待用户提出** |
| **F 轮(F1/F2/F3)** ✅ **代码完成 2026-09-14(待用户走查)** | F1 接管已有安装(§6.1 清单驱动四型探测 + `install.ts` 的 assert/probe/adopt/forget、store `origin`、`install:adopt`/`install:forget`/`dialog:pick-dir`、已安装页接管面板);F2 受管 PATH 条目并入检测表(环境页去过滤 + 「受管」标签 + 勾选禁用);F3 系统环境变量开关(§7.1.1 core 闸门/保护名单 + `env.ps1` System 三函数 + 设置页开关 + 环境页系统变量区 + `env:system-*` 三通道) | 达成:`pnpm typecheck` 两段干净、`pnpm test` **154/154** 绿(基线 111,新增 adopt 22 + env 10 + catalog 2 + 契约 2 + 渲染 4)、`pnpm dist` 四段全绿出 `devkit-setup-0.2.0.exe`(**81.2 MB**)。**待用户走查**:①接管一个真实既有安装(如 `C:\Program Files\nodejs`)→ 切换 → 移出登记;②管理员身份开启 F3 开关并新增/删除一个自定义系统变量(内置变量应被拒) |
| **F4(一星工具批量收录)** ✅ **完成并经用户验收、已推送 2026-09-17(`fef8a39`)** | 六份 catalog(MinGit/VS Code/Python/IDEA/PyCharm/DBeaver)+ `catalog.ts` 泛化(jsonApi/latestRedirect、rawVersion 自然序、aliases、maxVersions、versionPolicy.exclude、pinnedHash/discoveredSidecar、binName、空 rootDir 平铺)+ 解压改 PowerShell Expand-Archive(§7.5) | 达成:`pnpm test` **166/166**、`pnpm dist` 四段全绿出 `devkit-setup-0.3.0.exe`(81.2MB);四 pinned 工具真机 e2e 全过,python pinned 与官方整包逐一比对一致;用户走查通过后推送 |
| **F5(二星第一批收录)** ✅ **代码完成 2026-09-19(待用户走查 → push → Release)** | 三份 catalog(gradle/go/sqlite)+ `catalog.ts` 扩展(listScan.shape=`array` + pick/versionRegex、checksum.kind=`discoveredInline`、listKind=`regexPage`)+ 华为云 SPA 改版验真方法(PK 魔数/Range 206);未收录评估归档 BACKLOG「F5 未收录工具」 | 达成:`pnpm typecheck` 两段干净、`pnpm test` **170/170** 绿(基线 166,新增 array/regexPage/discoveredInline + schema 守门 + catalog 定稿门禁扩 12 工具)、`pnpm dist` 四段全绿出 `devkit-setup-0.4.0.exe`;三工具真机 e2e 全过(gradle 9.7.1 / go 1.27.1 / sqlite 3.53.4,--version 实测) |
| **C 轮(C1/C2 界面改进)** ✅ **代码完成 2026-09-20(待用户走查 → 开 VPN push)** | C1 设置页「镜像源优先级」整段移除(`SettingsView.sourcePriority`/`ToolCardView.sourceIds` 契约删除,prefsFor 只留代理前缀;ghproxy 前缀三态挪入网络卡);C2 方向 A 环境页 HKLM 区收窄为"系统 PATH 只看+只追加一条"(§7.1.1 新口径:core `applySystemPathAdd`/`assertSystemPathEntry` 取代 `applySystemVarSet/Remove`,通道置换见 §8)+ 安全复查 Medium 修复(`readSystemVars` fail-closed) | 达成:`pnpm typecheck` 两段干净、`pnpm test` **173/173** 绿(基线 170,条目校验/只增不删/fail-closed①②/C1 契约形状/C2 DTO 与通道守卫)、`pnpm dist` 四段全绿出 `devkit-setup-0.4.0.exe`(SHA-256 `ECB71B…835B`,**与 GitHub Release v1.3.0 附件不同哈希 → 走查后定发版方式**);`/security-review` 复跑:1 Medium 已修,无新增 High。**待用户走查**:①设置页无优先级区、前缀三态仍可用;②环境页 HKLM 区=系统 PATH 条目列表;开开关+管理员追加一条 → 检测表与新条目即时可见 → Windows 设置里删掉即回归;③旧条目等值重复追加应零写入 |
| **C3(装什么管什么动态受管 PATH)** ✅ **完成并推送 2026-09-22(`e33a892`)** | §7.2.1:受管 PATH 条目随「在管工具」(installs 登记)动态生成 —— `install.ts` 三函数(`pathEntrySuffixOf`/`toolManagedContribution`/`envPlanForInstalls`)+ `InstallContext.env?` 接线 + `EnvPlan.javaHome` 可空 + `removeJavaHome`;四处消费(EnvAudit/EnvPrune 保护集/env:state/SetupPreview·Run)同源;UI 环境页 JAVA_HOME 行仅 jdk 在管时显示;`paths.envPlan`(固定三条)删除 | 达成:`pnpm typecheck` 两段干净、`pnpm test` **188/188** 绿(基线 173,新 15:动态计划/自动接入/自动断开/removeJavaHome);走查通过(实走安装·接管·卸载·移出登记与 JAVA_HOME 回收);`git push origin main && git push origin --tags`;未 bump 版本/未发 Release |
| **F6-impl(收录 4 工具:JMeter/Ant/Tomcat/.NET SDK)** ✅ **实现完成 2026-09-24(`cde3342`)** | §6 jsonApi array 增强(`root` 对象内嵌数组 / `assetNamePath` url 末段 / pick+fileRegex 过滤 exe / 哈希长度分派 128hex→sha512)+ 四份 catalog:jmeter/ant/tomcat=officialSidecar sha512(官方 archive 跨域)、dotnet=discoveredInline sha512(官方 releases.json 内嵌);**Tomcat 两级扫描前提作废**(dirIndex 版本目录 + fileUrl 模板拼 `v{ver}/bin/`) | 达成:`pnpm typecheck` 两段干净、`pnpm test` **192/192**(基线 188 + 4)、真机版本发现四工具全过 + ant/jmeter/tomcat 完整 install() 闭环、`pnpm dist` 四段全绿(重出 devkit-setup-0.4.1.exe)、走查通过;**已 Release v1.5.0(见 BACKLOG)R1 配方** |

## 14. 决策记录(备查)

- 2026-09-07 产品文档 §7 定为 Tauri(Rust);同日按"合适且易实现"修订为 **Electron+全栈 TS**,Tauri 降为性能不达标时的迁移备选。迁移缝 = §4 的 core/ 隔离与 §8 IPC 契约,UI 代码可 100% 保留。
- 同日:存储从 SQLite 降为 JSON repository(§9),动因:回避原生模块 rebuild 链,数据量不需要 SQL。
- 2026-09-24(F6-impl):收录工具推 **16 个**。四项收录全部复用既有能力(第 N 份 jsonApi/officialSidecar 清单),仅 jsonApi array 补三处向后兼容增强;**.NET SDK 因无国内镜像,首次出现"官方单源 + discoveredInline"组合**(镜像优先支柱作例外,包 ~200MB)—— 收录依据=官方直连快 + 官方发布元数据内嵌 SHA-512 权威。
- 2026-09-21(M3 决策 A"固定 3 条"被推翻):受管 PATH 条目改为**随在管工具(installs 登记)动态生成**
  (»装什么管什么«,§7.2.1);安装/接管自动接入、卸载/移出登记自动断开;JAVA_HOME 只在本工具管理 JDK
  时写入(解决"装了 Maven 没装 JDK → JAVA_HOME 悬空");切换版本仍零 PATH 变动。回滚语义维持快照全量恢复。
