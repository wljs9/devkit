# 开发环境管理器 — 需求分析

> 版本:v0.1(讨论稿) 日期:2026-09-07

## 1. 背景与痛点

1. **下载难**:JDK、Python、Node、Maven、数据库等工具官网多在国外,下载慢;要在一堆页面里找对平台/架构的安装包(如 win-x64 zip vs msi)。
2. **镜像不会用**:国内镜像(华为云、清华 TUNA、阿里、中科大、腾讯)其实都有这些软件,但各镜像目录结构互不相同、URL 规则不统一,只能人工翻目录。
3. **环境变量失控**:每装一个软件就往 PATH 里加一条,久而久之 PATH 冗长、重复、顺序错乱、指向已删除目录;不同项目需要不同版本时只能手动改来改去;卸载后残留污染。

## 2. 产品目标

**一句话**:一个 Windows 上的「开发工具与环境变量统一管理」软件 —— 从国内镜像高速下载、可携化安装、多版本切换,并且让环境变量永远保持干净、可回滚。

**非目标(v1 不做)**:
- 不做 IDE / 编辑器;
- 不管理语言包管理器(pip / npm / maven 依赖本身,只管理运行时与工具本体);
- 不做企业团队分发与内网制品库;
- 不做容器编排(Docker Desktop 本体可以纳入"工具目录",但其内部不管)。

## 3. 目标用户与核心场景

主用户:Windows 11 上频繁搭建/调整开发环境的开发者(即你本人),后续可推广到有同样痛点的同事。

| # | 场景 | 期望流程 |
|---|------|---------|
| S1 | 新机初始化 | 一条命令/一个清单,批量装好 JDK17 + Maven + Node20 + Python3.12 + MySQL8,半小时内完成 |
| S2 | 多版本并存 | 项目 A 用 JDK 8、项目 B 用 JDK 21,随时切换且互不影响 |
| S3 | 升级/回滚 | Node 20 → 22 升级,验证失败一键回到 20 |
| S4 | 卸载清理 | 删除旧 JDK 8 后,PATH、文件、相关变量无任何残留 |
| S5 | 环境体检 | 一键审计 PATH:重复项、失效项、被第三方安装器偷偷写入的项 |

## 4. 功能需求(FR)

### FR-1 镜像源与工具目录
- 内置镜像列表:华为云 `mirrors.huaweicloud.com`、清华 `mirrors.tuna.tsinghua.edu.cn`、阿里、中科大等,**声明式配置**(JSON/YAML 目录清单),可增删改;
- 每个工具一份元数据:版本列表获取方式、各镜像上的目录规则、文件名匹配规则(platform/arch/扩展名)、校验和来源、下载回退顺序;
- 源可用性探测:定期或手动检测各镜像 URL 是否有效,失效时给出明确提示与替代源。

### FR-2 搜索与浏览
- 按名称/分类(语言运行时、构建工具、数据库、命令行工具…)浏览目录;
- 展示某工具的可用版本树(来自镜像目录实时解析 + 本地缓存)。

### FR-3 下载管理
- 多镜像并发测速选优、失败自动回退;
- HTTP Range 断点续传、进度/速率显示、可暂停取消;
- SHA-256 校验;镜像不提供校验和时,从官网获取或由用户确认后跳过(降级需警告)。

### FR-4 安装管理
- **优先可携化(portable)**:zip/tar 解压到统一根目录 `<DevRoot>\tools\<名称>\<版本号>\`,不写注册表、不跑安装向导;
- 仅有安装器的软件(Python 官方安装包、PostgreSQL):支持声明静默安装参数(如 Python 的 `InstallAllUsers=0 PrependPath=0 TargetDir=...`),安装后立即接管其 PATH 项;
- 安装产物登记入库:记录来源 URL、校验和、时间、装到了哪。

### FR-5 多版本与切换
- 同一工具多版本并存;
- `use <tool>@<version>` 设为当前版本(全局默认);
- (进阶)项目级覆盖:在某目录/终端会话内使用指定版本。

### FR-6 环境变量管理(核心卖点)
- **单入口策略**:PATH 里理想状态只保留 1~2 条本工具管理的目录(如 `D:\dev\bin`);版本切换通过 Windows Junction(目录联接,普通用户即可创建,无需管理员)指向当前版本,PATH 本身几乎不再变动;
- 读写走 **HKCU(用户级)注册表**,默认不需要管理员权限;动系统级 PATH 必须显式确认;
- **操作历史 + 一键回滚**:每次写环境变量前自动备份原值,记录操作日志;
- 冲突/卫生检查:重复条目、指向不存在目录的条目、顺序异常检测;一键清理(仅清理确认失效项,外部条目先提示);
- (进阶)环境变量**方案(Profile)**:如"工作""个人",整套变量组合一键切换。

### FR-7 卸载与清理
- 删除某个版本:移除文件 + 自动回收其环境变量改动,删除前预览影响面并要求确认;
- `doctor` 命令:全盘扫描由本工具管理 vs 外来污染的环境变量项。

### FR-8 状态总览
- 已安装清单、各工具当前版本、PATH 快照及变更 diff 视图。

### FR-9 扩展(后续)
- 导入/导出环境清单(manifest),新机一键复刻;
- 代理设置;软件自更新;主题/i18n。

## 5. 非功能需求(NFR)

- **权限**:尽量免管理员(用户级注册表 + Junction + 解压安装);
- **平台**:Windows 10/11 x64 优先,ARM64 与 macOS/Linux 视后续决策;
- **健壮性**:任何注册表/PATH 写入前必备份,失败自动回滚;镜像不可达 30s 内回退;
- **隐私**:纯本地工具,目录清单可离线缓存,不上传用户环境信息;
- **安全**:校验和不匹配默认拒装;长期考虑软件数字签名(避免杀软误报);
- **效率**:下载占满带宽不受限,但默认并发 ≤4;CLI 可进脚本/CI。

## 6. 同类方案对比与差异化

| 方案 | 优点 | 不满足本需求之处 |
|------|------|----------------|
| SDKMAN! | JVM 生态 CLI 体验极佳 | 只支持 Unix/macOS;不管 Windows 环境变量 |
| asdf / mise | 插件丰富;mise 的 shim 思路好 | 源全部在 GitHub/官网,国内下载慢;Windows 支持一般 |
| Scoop | Windows 可携安装 + shims 单 PATH 入口,**思路最接近** | manifest 指向 GitHub/官网,国内网络体验差;无国内镜像目录;缺"环境变量审计/回滚" |
| Chocolatey / winget | 软件全 | 以系统级安装为主,装完 PATH 谁来写不管;版本切换弱;多版本并存差 |
| nvm-windows / pyenv-win | 单工具好用 | 碎片化,每个工具一个 manager,仍然各占各的环境变量位 |

**差异化定位** = 国内镜像优先目录 + 可携安装/多版本切换 + 环境变量卫生学(单入口、历史回滚、审计清理)三合一。

## 7. 技术方案候选(待讨论决策)

| 方案 | 契合点 | 顾虑 |
|------|--------|------|
| A. C#/.NET 8 + WPF 或 Avalonia | 注册表/Junction/进程调用是 Windows 原生领域,C# 最顺手;单文件发布 | 跨平台 GUI 一般;需要熟悉 C#(或现学) |
| B. Rust + Tauri v2 | 体积小性能好,Tauri 前端可用 Web 技术做 GUI;HTTP/解压生态够用 | 学习曲线;Windows API(Junction、HKCU)样板代码多 |
| C. Go + CLI 优先(Fyne/Wails 可选) | 单二进制、交叉编译、HTTP 简单 | GUI 生态弱 |
| D. Node.js + Electron | 你熟悉 Node;npm 生态拿现成轮子多 | 包体大、内存高,为一个"环境管家"略重 |

> 决策因素:你最想/最熟哪门语言?GUI 优先级多高?Windows API 操作密度很大 —— 若求省心,A(.NET)与 B(Rust+Tauri)最贴合,但"用自己的技术栈练手"也是合理诉求。

## 8. MVP 建议(先跑通核心闭环)

1. 目录仅收录:**Java(Temurin/Corretto)+ Node + Maven + Python**;
2. 镜像源:**华为云 + 清华**(先做真实目录结构探测,验证 URL 规则);
3. 下载(断点续传 + SHA256)→ 可携解压安装到 `<DevRoot>\tools\`;
4. Junction 版本切换,PATH 仅一条 `<DevRoot>\bin`;
5. 环境变量:操作历史 + 备份 + 一键回滚;
6. **CLI 形态**(GUI 后置,界面留给第二阶段)。

暂缓:数据库类(安装器复杂)、静默安装接管、Profile 切换、GUI。

## 9. 风险与约束

| 风险 | 应对 |
|------|------|
| 镜像覆盖不均:如 Redis 无官方 Windows 版,部分软件国内镜像不托管 | 目录标注"可行性";官方直连 + 代理兜底;必要时推荐替代品 |
| 镜像目录结构/URL 规则变更 | 声明式目录 + 定期探测 + 清晰报错,改配置不改代码 |
| 校验和来源不统一 | 校验分级策略:强校验 → 官网获取 → 用户确认跳过 |
| 第三方安装器仍会直写 PATH 造成污染 | 审计功能定期发现外来条目;junction 策略使我方几乎不动 PATH,污染可辨 |
| 环境变量改坏导致系统异常 | 写前备份 + 回滚;默认只碰用户级 HKCU |
| 安装路径含中文/空格、长路径 | 强制建议纯 ASCII 根目录(如 `D:\dev`) |

## 10. 初步可行性调研(需实测验证)

| 工具 | 理想形态 | 国内镜像可能来源 | 可携化 | 备注 |
|------|---------|----------------|--------|------|
| JDK | zip | 清华 github-release 镜像(Adoptium Temurin)、华为;国产龙芯/龙芯DK→毕昇/龙芯/Dragonwell(Kona、Dragonwell 在 Gitee) | ✅ | JAVA_HOME 由 junction 管理 |
| Node.js | zip(win-x64) | 华为 `/nodejs` 目录(待验证)、官方 nodejs.org 国内速度尚可 | ✅ | 最理想的 MVP 试点 |
| Maven | zip | 华为/清华 apache 目录(稳定) | ✅ | 无安装过程,纯 PATH 型 |
| Python | 官方只有 .exe 安装器(embeddable zip 不适合开发) | 清华 `/python-release`(源码);安装包本体仍需 python.org | ⚠️ | 需静默安装参数支持,或考虑用 uv 分发 CPython(可携,现代方案) |
| MySQL | zip archive | 华为镜像有 MySQL Downloads 目录(待验证) | ✅(zip 版) | 还需初始化数据目录/服务注册,复杂度中等 |
| PostgreSQL | EDB 安装器 | 源码有镜像,Windows 二进制基本只有官方 | ⚠️ | 暂缓 |

> 结论:**Node + Maven + JDK 适合打第一仗**,Python 次之(或走 uv 路线),数据库类放第二阶段。

## 11. 待确认问题

- Q1 产品形态:CLI 优先?GUI+CLI?还是必须 GUI?
- Q2 平台范围:只做 Windows?
- Q3 技术栈:有无想练手/最熟的语言?
- Q4 安装根目录约定:`D:\dev\` ?由谁定?
- Q5 版本切换:全局切换是否够?要不要"进入某目录自动切换"(需 shell 集成)?
- Q6 数据库范围:MySQL/PostgreSQL/Redis 哪些是刚需?
- Q7 是否需要内网/私有镜像支持?
